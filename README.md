# Simple File Encryption Tool

Client-side file encryption using **XChaCha20-Poly1305** (via libsodium).

## Features
- CLI + React UI
- Real progress updates via EventEmitter
- Secure password-based encryption (Argon2id)
- TypeScript + styled-components

## Run CLI
```bash
npm install
node cli.js
